const myObject = {

    js:'javascript',
    cpp : 'C++',
    rb : "ruby",
    swift: "Swift by apple"
}
// For in loop is also used in other places also
for (const key in myObject) {
    console.log(key);  // Print only keys

    // For values console.log(myObject[key])
}

// 
const programming = [ "Js" , "CPP " , "PY ", "Java"]

for (const key in programming) {
   console.log(key);
   //Array also have Key starting from zero  and so on value in array 
   // Can be Accessed using [] brackets
   // That's Why Object was Created

}

// For


